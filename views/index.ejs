<html>

<head>
    <title>
        <%= title %>
    </title>
</head>

<body>
    <%- include('navbar', {link: {href: "/new" , text: "Create New Message" }}) %>
        <h3>Messages</h3>
        <nav>
            <% if (messages) { %>
                <% messages.map((message)=> { %>
                    <li>
                        <a href="/messages/<%= message.id %>">
                            <span>
                                <%= message.user %>
                            </span>
                        </a>
                    </li>
                    <% })} %>
        </nav>
</body>

</html>